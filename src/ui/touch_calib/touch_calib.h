/**
 * @file ui/touch_calib/touch_calib.h
 * @brief Compatibility redirect.
 *
 * Driver source files in this library include touch_calib via the path
 *   "ui/touch_calib/touch_calib.h"
 * (matching the cnc_interface project layout).
 *
 * This redirect header keeps the driver files unmodified while the actual
 * implementation lives at src/touch_calib/touch_calib.h.
 */
#include "../../touch_calib/touch_calib.h"
